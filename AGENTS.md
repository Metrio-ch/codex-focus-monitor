# 项目工作文件存放规则

本文件定义当前项目的文件存放、整理与清理边界。项目原有目录、运行约定和本文件中的项目专属规则继续有效；通用规则用于保持项目长期可读、可维护、可交付。

<!-- BEGIN UNIVERSAL PROJECT STORAGE POLICY v1 -->
## 通用项目文件存放规则

### 一、根目录

- 项目根目录必须保留 `AGENTS.md`，用于记录项目规则与文件存放约定。
- 根目录只保留项目入口、说明、必要配置、清单文件和无法迁移的兼容文件。
- 已有 `README.md`、源码、数据、文档、测试、构建和交付目录时，沿用项目原生结构；不要为了统一外观强行改名或搬迁。
- 新增目录应服务于实际用途，不为形式创建空目录。

### 二、文件归位

- 源码与自动化：放入项目已有的 `src/`、`app/`、`scripts/`、`tools/` 或同类目录。
- 说明与设计：放入 `docs/`、项目既有文档目录或编号式知识目录。
- 原始与业务数据：放入 `data/`、`input/` 或项目既有数据目录；原始数据保持只读，不与加工结果混放。
- 测试与验证：放入 `tests/`、`test/`、`90-测试与验证/` 或项目既有测试目录。
- 中间文件：放入 `work/`、`tmp/`、`runs/` 或项目明确指定的临时目录；这些文件必须能够重建，不能作为唯一成品。
- 最终交付物：放入 `outputs/`、`30-成果输出/`、`release/` 或项目明确指定的成果目录；最终回复优先链接该目录中的文件。
- 历史版本：放入 `archive/`、`70-归档/`、`99-归档/` 或项目既有归档目录，并保留日期或版本号。
- 密钥与隐私数据：只放在项目已忽略的安全位置，例如 `.env`、`.secrets/` 或系统密钥存储；不得写入 `AGENTS.md`、交付物或可提交文件。

### 三、整理原则

1. 移动文件前先搜索相对路径、绝对路径、脚本入口和外部配置中的引用；同步更新并验证后再移除旧路径。
2. 有 Git 的项目先检查工作树；有大量未提交改动时，只做不会混淆现有变更的最小整理。
3. 同名成品不得直接覆盖；使用清晰的日期、版本号或状态后缀。
4. 草稿、抓取原文、渲染预览、一次性脚本和测试数据不得散落在根目录。
5. 项目已有的专属目录规范优先；通用规则用于补齐缺口，不放宽删除、覆盖、隐私和密钥安全边界。

### 四、清理边界

- 可直接清理：`.DS_Store`、编辑器交换或备份文件、源码目录中的 `__pycache__/` 与 `.pyc`、已由正式文件替代的渲染预览、确认未被占用的陈旧锁文件、确认为空的临时目录。
- 需先核查：`node_modules/`、虚拟环境、模型、下载缓存、`build/`、`dist/`、`target/`、日志、运行时数据库、原始数据、备份和归档。只有确认可重建、未被运行中流程使用、且不是唯一副本时才可删除。
- 禁止误删：最终交付物、业务原件、唯一原始数据、唯一备份、项目源码、当前运行时依赖和用户已有文件。
- 清理后记录删除类别、数量与释放空间，并验证项目入口和最终交付物仍存在。

### 五、新项目初始化

1. Codex 新增项目或首次进入项目时，先在登记的项目根目录创建或合并 `AGENTS.md`。
2. 通用模板固定为 `~/.codex/templates/project-AGENTS.md`（当前用户主目录）。
3. 根目录已有 `AGENTS.md` 时，只补入缺失的通用规则块，保留全部项目专属内容，禁止整文件覆盖。
4. 只处理 Codex 登记的项目根目录；日期型临时工作空间、依赖目录、子模块和构建产物不自动套用项目模板。

<!-- END UNIVERSAL PROJECT STORAGE POLICY v1 -->

## 项目专属规则

- Kayla Monitor 的运行时数据只能写入用户的 `~/Library/Application Support/Kayla Monitor`，不得提交到仓库。
- 全局 hooks 安装必须保留现有 handler，且在实际执行前单独取得用户授权。
- 任务完成状态只能由用户手动确认；`Stop` 事件只能映射为“等你判断”。
